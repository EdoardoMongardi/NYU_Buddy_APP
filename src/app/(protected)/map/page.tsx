/**
 * Route stub — the actual map UI lives in the protected layout
 * as a singleton (CSS-toggled, never unmounted) to minimize Mapbox loads.
 */
export default function MapPage() {
  return null;
}
